#include<lib/common.h>

#define METHOD findMedianSortedArrays

class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        m = len(nums1);
        n = len(nums2);
    }
};

#ifdef DEBUG
int main() {
    Solution s;
    vi x = {};
    vi y = {};
    print(s.METHOD(x, y));
    return 0;
}
#endif