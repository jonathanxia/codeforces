#include <lib/common.h>

void solve() {
    ll n; cin >> n;
    vl L(n);
    vl R(n);
    rep(i, 0, n - 1) {
        cin >> L[i] >> R[i];
    }

    
}

int main() {
    init();
    int t; cin >> t;
    cep(t) {
        solve();
    }
    return 0;
}